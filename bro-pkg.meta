[package]
description = State of processes and their local connections for osquery hosts.
tags = osquery, state, host, connection
version = 0.3.2
depends =
  zeek-osquery-framework ~0.3.2
  zeek-osquery-state ~0.3.2
script_dir = scripts
